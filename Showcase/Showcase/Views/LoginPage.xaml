<?xml version="1.0" encoding="utf-8" ?>
<base:BasePage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:base="clr-namespace:Showcase;assembly=Showcase"
             x:Class="Showcase.Views.LoginPage"
             Title="{Binding Title}">
    <StackLayout 
        HorizontalOptions="Center"
        VerticalOptions="CenterAndExpand">
        <Label Text="Efetue o login com sua rede social." TextColor="Black" FontSize="Medium" />
        <Button Command="{Binding LoginCommand}" Text="Conectar com Facebook" BackgroundColor="#3b5998" FontSize="Medium" TextColor="White"></Button>

    </StackLayout>
</base:BasePage>